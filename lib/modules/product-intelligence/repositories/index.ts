/**
 * Repositories — Barrel export
 *
 * Sprint 11A: All repositories throw on access (tables don't exist yet).
 * Real data access wired when migrations are applied.
 */

export { kpiRepository } from './kpi-repository'
export { forecastRepository } from './forecast-repository'
export { scoringRepository } from './scoring-repository'
export { recommendationsRepository } from './recommendations-repository'
